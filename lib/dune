(library
 (name my_lang)
 (libraries core yojson)
 (preprocess
  (pps ppx_jane))
  (modules ast types typeinfer bytecode compiler vm gc lexer parser eval my_lang wasm_backend wasm_binary package_manager lsp_server))

(menhir
 (modules parser))

(ocamllex lexer)
