(library
 (name my_lang)
 (libraries core)
 (preprocess
  (pps ppx_jane))
  (modules ast types typeinfer bytecode compiler vm lexer parser eval my_lang wasm_backend))

(menhir
 (modules parser))

(ocamllex lexer)
